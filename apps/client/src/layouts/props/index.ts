/**
 * Props Barrel Export
 *
 * Central export point for all layout props interfaces.
 * This allows for cleaner imports throughout the codebase.
 *
 * @module layouts/props
 */

export type {
  MainLayoutProps,
  ContextMenuState,
  DrawingToolbarProps,
  DrawingBoardProps,
  ToastState,
  RollLogEntry,
} from "./MainLayoutProps";
